# User Guide

## Configuration

See [SimulationConfig](api/config.md) for the full Pydantic schema.

Key config sections: `circuit`, `geometry`, `fluid`, `physics`, `diagnostics`.

## Running Simulations

See [SimulationEngine](api/engine.md).

## Diagnostics

See [Diagnostics API](api/diagnostics.md) for neutron yield, energy balance, and scaling laws.

## Validation

See [Validation API](api/validation.md) for experimental comparison and V&V suites.
