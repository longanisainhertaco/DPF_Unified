# First-Principles WP-N1 / WP-N4 Runtime-Authority Submission

Date: 2026-05-18

Submitting team: Cortana multi-agent implementation team (Claude Opus 4.7 lead +
5 delegated dpf-domain agents, plan-first per the agent execution playbook).

Against: `docs/FIRST_PRINCIPLES_CODEX_AGENT_AUDIT_AND_NEXT_INSTRUCTIONS_2026_05_18.md`
("Next Submission Required" — a WP-N1/WP-N4 runtime-authority packet).

Self-assessed verdict offered: **`accept_engineering_progress`**. The power port
now emits a complete *candidate* five-term ledger with named Auluck domain masks,
sign convention, step-consistent time-centering, residual, and six passing
negative tests — and **stays non-accepting**. The certificate gate and all
first-principles gates remain blocked. No first-principles acceptance is claimed.

---

## 1. Summary Of Intent

Execute the Codex "Next Submission Required" — items 1–7 — without starting a new
broad plan. The round closes the runtime-authority hygiene tail (A-1, A-2, A-3),
delivers the WP-N1 power-port domain + five-term ledger (A-5), and the WP-N4
segmented whole-shot runner design with first restart-equivalence evidence (A-4).
A-6/A-7/A-8 (startup, geometry materials, closures/neutron) are **explicitly
deferred** per the Codex doc's own scoping ("the next submission should be a
WP-N1/WP-N4 runtime authority packet").

WP-N1 was run **plan-first**: a Plan agent produced a source packet from
`KnowledgeReference/`; the lead verified its load-bearing Auluck citations against
the KR file before authorizing implementation.

---

## 2. Changed Files

### Source / scripts

| File | Change | Finding |
| --- | --- | --- |
| `src/dpf/fields/source_geometry.py` | `build_auluck_omega_domain` — 4-label disjoint exhaustive Ω partition with per-label mask SHA256, counts, bounds, source refs | A-5 / WP-N1 |
| `src/dpf/fields/hybrid_stepper.py` | volume `J·E` re-scoped to `omega_volume_cells`; wall-Poynting + stored-EM-delta ledger terms | A-5 / WP-N1 |
| `src/dpf/fields/hybrid_simulator.py` | five-term `power_port_ledger` telemetry; sign convention + step-consistent time-centering | A-5 / WP-N1 |
| `src/dpf/first_principles/power_port.py` | five-term ledger packet, residual policy, negative-test policy | A-5 / WP-N1 |
| `src/dpf/first_principles/segmented_whole_shot.py` *(new)* | segmented whole-shot runner: planner, runner, staged restart-equivalence | A-4 / WP-N4 |
| `src/dpf/cli/main.py` | `dpf experimental-segmented-whole-shot` command | A-4 / WP-N4 |
| `src/dpf/first_principles/manifest.py` | `sha256_of_file_soft`, `source_packet_hashes_from_references` | A-1 |
| `src/dpf/first_principles/runner.py` | `_build_manifest` populates `source_truth_index_sha256` + `source_packet_hashes` | A-1 |
| `scripts/audit_first_principles_artifacts.py` | linter check C7 (`manifest.provenance_complete`); EXEMPT disposition for archived / non-authority artifacts with status reason | A-2 |
| `docs/DPF_REQUIREMENTS_BASELINE.md`, `docs/SRS_TRACEABILITY_MATRIX.{csv,json}` | RTM rows DPF-PHYS-020..026 (WP-N1..N7) | A-3 |

### Tests

| File | Change |
| --- | --- |
| `tests/test_first_principles_power_port.py` *(new)* | WP-N1 power-port tests + 6 negative tests (12 tests) |
| `tests/test_first_principles_segmented_whole_shot.py` *(new)* | WP-N4 segmented runner tests (20 tests) |
| `tests/test_first_principles_artifact_linter.py` | C7 + archive-policy coverage |
| `tests/test_first_principles_manifest.py` | provenance-helper coverage |
| `tests/test_srs_traceability_export.py` | requirement-count assertion made dynamic (`== len(rows)`; was a stale `== 48`, red since the baseline grew) |

### Docs

`docs/ssr_audit_2026_05_18/WP-N1_power_port_source_packet.md` *(new — the
lead-verified plan-first source packet)*; this submission;
`results/audit_*.json` *(3 regenerated)*; source-truth/module-vetting dated docs
*(regenerated by the gate scripts)*.

Prior commit `466a0a5` (`ci(first-principles): activate audit gate + Codex
audit-time fixes`) delivered "Next Submission" item 1 — a clean commit with the
CI `first-principles-audit` gate active.

---

## 3. Findings Addressed

| Codex finding | Status | Evidence |
| --- | --- | --- |
| A-1 manifest provenance incomplete | fixed | `_build_manifest` populates `source_truth_index_sha256` + `source_packet_hashes`; the 3 regenerated artifacts report `manifest.provenance_complete: true` |
| A-2 linter scope too narrow | fixed | C7 fails on `provenance_complete` false/missing; archived + non-authority artifacts are EXEMPT-with-reason, not silently skipped |
| A-3 SRS/RTM stale | fixed | 7 rows DPF-PHYS-020..026 added to the requirements baseline; matrix regenerated (62 → 69 requirements) |
| A-4 12 µs path infeasible | progressed | segmented whole-shot runner + CLI; staged restart-equivalence proven bit-identical; full 12 µs remains an honest compute-wall blocker |
| A-5 power port (first physics blocker) | progressed, non-accepting | named Auluck Ω domain + five-term ledger + sign/time-centering + residual policy + 6 negative tests; `can_support_power_port_acceptance: false` |
| A-6 startup / A-7 geometry / A-8 closures+neutron | deferred | per the Codex doc scoping the next submission to WP-N1/WP-N4 |

---

## 4. Source Evidence (WP-N1, lead-verified against KnowledgeReference)

| KR source:lines | Claim | Implemented in | Status |
| --- | --- | --- | --- |
| `auluck-2021-dpf-circuit-element.md:196-201` | `V = (1/I)∫_Ω(J·E)d³r` — terminal voltage as field power / current | `power_port.py`, `hybrid_stepper.py` (term 2) | VERIFIED — lead opened the file; prose legible (eq-1 glyphs OCR-garbled, prose relied on) |
| `auluck-2021-dpf-circuit-element.md:203-209` | Ω is the current-carrying domain; the cathode-plate/insulator/squirrel-cage source interface is **excluded** | `source_geometry.py` `build_auluck_omega_domain` | VERIFIED verbatim |
| `auluck-2021-dpf-circuit-element.md:215-257` | Σ toroid; source-interface Poynting flux = `I·V` | Ω domain masks; ledger terms 1, 3 | VERIFIED verbatim |
| `2019nrlplasma-formulary-037290d4.md:1880-1888` | Poynting theorem; signed `J·E`; `W=½∫(H·B+E·D)dV` | ledger sign convention; term 5 | VERIFIED (per source packet) |
| `auluck-2021-dpf-circuit-element.md` eq 5/6 | electrode/moving-boundary integrand | term 4 — **NOT implemented from this source** | OCR-illegible (gap G1) — term 4 is a labeled non-independent closure estimate |

Full packet: `docs/ssr_audit_2026_05_18/WP-N1_power_port_source_packet.md`.

---

## 5. Commands Run

| Command | Result |
| --- | --- |
| `ruff check` (21 round-touched files) | All checks passed (1 import-order autofix applied) |
| `pytest tests/test_first_principles_*.py tests/test_hybrid_3d_*.py tests/test_srs_traceability_export.py -q` | **243 passed** |
| `pytest tests/test_infrastructure_consolidated.py tests/test_snowplow_consolidated.py tests/test_srs_traceability_export.py -q` | **494 passed, 2 skipped, 3 xfailed** |
| `pytest tests/test_first_principles_power_port.py -q` | **12 passed** (WP-N1: 6 negative + 6 ledger/domain) |
| `pytest tests/test_first_principles_segmented_whole_shot.py -q` | **20 passed** (WP-N4) |
| `python scripts/audit_first_principles_artifacts.py 'results/*.json'` | 45 scanned — 3 first-principles, 3 passed, 0 failed, 11 exempt — **PASS** |
| `python scripts/verify_first_principles_source_truth_exhaustion.py --strict` | `exhausted=true`, exit 0 |
| `python scripts/verify_first_principles_module_source_vetting.py --strict` | `strict_passed=true`, 289 modules, exit 0 |
| `git diff --check` | clean |

---

## 6. Artifacts

| Path | Purpose | SHA256 |
| --- | --- | --- |
| `results/audit_first_principles_3d_smoke.json` | 3-D first-principles 2-step smoke (full provenance) | `37874c306613109c57527076aea2422d4397b31eff22d6bc96ff876803467f61` |
| `results/audit_experimental_whole_shot_smoke.json` | whole-shot 20-step smoke (full provenance) | `30800b65eeae4d22a288cc87047979bdeeadcbc6ed845083365fda14a517da95` |
| `results/audit_limiter_proof_auluck_power_port_1us_2026_05_18.json` | 1 µs source-sign power-port run (full provenance) | `46870e945aebad6fc9025058bebae3343bce5221bd6c263dca70bc1f9d373470` |

All three regenerated from this round's HEAD; all three pass the artifact linter
including the new C7 `provenance_complete` check.

---

## 7. Negative Tests

WP-N1 power-port — 6 negative tests (`tests/test_first_principles_power_port.py`),
all passing: N1 sign reversal, N2 wrong domain (source interface inside Ω), N3
omitted electrode work, N4 low-current `P/I` singularity, N5 first-step fallback,
N6 default-mode leakage. WP-N4 — segmented runner negative coverage: mismatched-deck
checkpoint fail-closed, wall-time-cap partial-run labelling, ledger-counts-executed-
not-planned. Linter — C7 fails `provenance_complete` false/missing; archived /
non-authority artifacts exempt-with-reason.

---

## 8. Remaining Blockers

- **G1** — Auluck eq 5/6 (electrode/moving-boundary integrand) is OCR-illegible in
  the KR extract. Term 4 is implemented as a labeled non-independent closure
  estimate (`electrode_interface_work_J__closure_estimate_not_independent`); the
  residual therefore closes trivially and **cannot** support acceptance. Needs a
  legible rendering of Auluck eq 5–6.
- **G2** — no `KnowledgeReference/` source prescribes a power-port residual
  tolerance; `accepted_residual_tolerance: not_attached`. Acceptance blocked.
- **G3** — no source-backed high-order time-centering; only step-consistency claimed.
- **G4** — PF-1000 material masks (`wall_material_faces`) depend on A-7 reviewed
  geometry; currently `geometry_candidate_not_reviewed`.
- **G5** — hybrid-PIC `U_DPF` is a magnetic-flux relation, not Auluck volume `J·E`;
  not conflated.
- **WP-N4** — full 12 µs source-sign run (~1.2e8 steps) is a compute-wall blocker;
  staged restart-equivalence is proven, the full run is not attempted/faked.
- **Pre-existing (A-9)** — `src/dpf/first_principles/gv_waveforms.py` carries 2
  broad-lint errors (`SIM117`, `B905`); untouched this round, not a regression.
- A-6 / A-7 / A-8 — startup BVP, PF-1000 material geometry, closure/neutron
  authority remain blocked, deferred to subsequent submissions.

---

## 9. AI / Tool Disclosure

- **Tool:** Claude Opus 4.7 (Cortana) as lead + 5 delegated opus dpf-domain agents
  (`dpf-mhd-physicist`, `dpf-engine-architect`, `dpf-validation-engineer`).
- **What it produced:** the WP-N1 source packet, the implementation code, tests,
  RTM rows, and this report.
- **Verification:** WP-N1 ran plan-first — the source packet's load-bearing Auluck
  citations were re-opened and verified against `KnowledgeReference/` by the lead
  before implementation was authorized.
- **AI suggestion rejected for lack of local source support:** the Auluck eq 5/6
  electrode-work integrand was **not** implemented — the KR extract is OCR-
  illegible there; the term is a labeled non-independent closure estimate rather
  than invented physics (gap G1).
- No reduced model (Lee/RADPF/snowplow/scaling) drives any first-principles result.
- No external web/paper material was used as scientific authority.
